services:
  - type: web
    name: workspace-app
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3000
      - key: ANTHROPIC_API_KEY
        sync: false
